package ucf.assignments;

import static org.junit.jupiter.api.Assertions.*;

class TodoListTest {
    //instantiates a `TodoList` object and calls TodoList("Test Title")

    private void edit_title_test(String new_title){
        //calls edit_title on the `TodoList` object
        //then checks if the new title corresponds with expected
    }

    private void add_item_test(){
        //calls add_item("This is a test message") with a default message inside
        //accesses `TodoList` to check if the default message corresponds to expected
    }

    private void rm_item_test(){
        //calls rm_item on the Item added by the method above
        //checks if the `TodoList` instance of this class is empty
    }

}