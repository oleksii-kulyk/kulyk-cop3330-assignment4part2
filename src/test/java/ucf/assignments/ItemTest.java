package ucf.assignments;

import static org.junit.jupiter.api.Assertions.*;

class ItemTest {
    //instantiates an Item object by calling Item(String default_message)

    private void set_description_test(){
        //calls set_description and overwrites description variable with a test string
        //then checks if the description string matches the expected test string
    }

    private void set_duedate_test(){
        //calls set_duedate and overwrites the due date with a hard coded test date
        //then checks if the due date matches with the expected hard coded value
    }

    private void set_complete_test(){
        //calls set_complete and sets the value of `complete` to true
        //then asserts equal to check if the value of set to true
    }
}